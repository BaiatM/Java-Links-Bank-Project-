package com.automation.test.pages;

public class AddNewCheckingAccountFunctionality {
    //	for pushing new codes, make sure the main is up to date then create a new branch with ur new codes, then push it with ->
    //
    //git push -u origin name of the branch
    //
    //then wait till its merged with main, now go back to main and pull the new changes

    //new changes on comments
}
